# API Configuration
# Copy this file to .env and set your actual credentials

# Bearer Token Authentication (optional - leave empty to disable)
API_BEARER_TOKEN=your-secret-bearer-token-here

# HTTP Basic Authentication (optional - leave empty to disable)
# Both username and password must be set to enable Basic Auth
API_BASIC_AUTH_USER=your-username
API_BASIC_AUTH_PASS=your-password

# Debug Endpoints (always available for ESP-IDF troubleshooting)
# No configuration needed - these endpoints help with HTTP client debugging
