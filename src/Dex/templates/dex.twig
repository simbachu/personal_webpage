{% extends "@shared/layout.twig" %}
{% import "@dex/monster_card.twig" as mc %}
{% import "@dex/tierlist.twig" as tl %}

{% block content %}
    {% if monster is defined %}
    {{ mc.monster_card(monster) }}
    {% elseif tierlist is defined %}
    {{ tl.tierlist(tierlist) }}
    {% endif %}
{% endblock %}
